name: Secret Test

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Print message
        run: echo "Hello"

      - name: Show Secret
        run: echo "Secret is ${{ secrets.MY_SECRET }}"
